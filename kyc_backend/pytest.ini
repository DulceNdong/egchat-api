[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    asyncio: tests async con pytest-asyncio
    integration: tests que requieren BD real
    unit: tests puramente unitarios
