fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
asyncpg==0.29.0          # driver async PostgreSQL
alembic==1.13.1
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # bcrypt hashing
python-multipart==0.0.9            # file upload
httpx==0.27.0                      # cliente HTTP async (screening, webhook tests)
python-dotenv==1.0.1
cryptography==42.0.5               # AES-256
boto3==1.34.93                     # S3-compatible storage (Supabase Storage)
supabase==2.4.2                    # Supabase SDK (storage upload)
slowapi==0.1.9                     # rate limiting

# ── Testing ───────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-mock==3.14.0
